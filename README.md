# HashCracker-Python

This Hash Cracker is made with Python. You need a passwords file, a hash of a password and you need the hash algorithm. Then it will find the Password if it exists in the file.

The usage for the program is: py HashCracker.py [Passwords File] [Hash] [sha1 sha256 sha512 md5]
